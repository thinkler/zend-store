<h1>Are you sure that you want to delete {{ item.title }}?</h1>

<form method="post" action="{{ url('item', {'action': 'delete', 'id': item.id}) }}">
    {{ forms.input('id', item.id, 'hidden') }}
    {{ forms.input('del', 'Yes', 'submit') }}
    {{ forms.input('del', 'No', 'submit') }}
</form>
